/**
 * TODO: Define the different kind of inputs to the request node.
 */
export interface XgeniaRequest {
  headers: {};
  body: string;
}

/**
 * TODO: Define the different kind of outputs on the response node.
 */
export interface XgeniaResponse {
  statusCode: string;
  headers: {};
  body: string;
}
