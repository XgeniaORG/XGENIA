/**
 * Dummy pro-nodes module for when pro-nodes are not available
 * This prevents webpack build failures when pro-nodes aren't present
 */

// Export a dummy module that will throw when accessed
module.exports = null;
